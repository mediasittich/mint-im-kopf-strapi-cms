{
  "routes": [
    {
      "method": "GET",
      "path": "/reviews",
      "handler": "reviews.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/reviews",
      "handler": "reviews.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/reviews",
      "handler": "reviews.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
